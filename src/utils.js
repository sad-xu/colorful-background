
/**
 * Add opacity and change color code style
 * 
 * 
 */
function colorFormat(code, opacity) {
  console.log(code, opacity)
  // if () { // hex

  // } else if () { // rgb

  // } else if () { // hsl

  // } else 
  return code
}

module.exports = {
  colorFormat
}
